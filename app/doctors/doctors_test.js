'use strict';

describe('myApp.DoctorsCtrl module', function() {

  beforeEach(module('myApp.DoctorsCtrl'));

  describe('doctor controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var doctorsCtrl = $controller('DoctorsCtrl');
      expect(doctorsCtrl).toBeDefined();
    }));

  });
});
